// 批量选择模式配置
export const BATCH_SELECT_CONFIG = {
  // 长按触发延迟 (ms)
  LONG_PRESS_DELAY_MS: 500,

  // 长按期间允许的移动阈值 (px)
  // 超过此阈值将取消长按
  MOVE_THRESHOLD_PX: 10,

  // 动画持续时间 (ms)
  ANIMATION_DURATION_MS: 200,
}
